# NixOS build outputs
result
result-*

# Age keys - never commit these!
*.txt
keys.txt
age-keys.txt

# Temporary files
*.tmp
*.bak
*.swp
*.swo
*~

# Editor files
.vscode/
.idea/
*.sublime-*

# OS files
.DS_Store
Thumbs.db

# Secrets - be specific about what's safe
# .age files are safe to commit (they're encrypted)
# secrets.nix is safe to commit (only contains public keys)
# Never commit plain text keys or age private keys
*.secret
*.key
*.txt
!*.age
!secrets.nix

# Development
.direnv/
.envrc.local

# Hardware specific (if you want to keep them out)
# devices/*/hardware-configuration.nix